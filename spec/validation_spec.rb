require 'spec_helper'

#describe 'callbacks' do
  #context "after_save" do

    #it "raise_error on normal save" do
      #expect{ Callbacker.new(name: 'Cally').save }.to raise_error StandardError
    #end

    #it "not raise error on save with false" do
      #expect{ Callbacker.new(name: 'Cally').save(false) }.not_to raise_error
    #end

    #it "not raise error on save with validate: false" do
      #expect{ Callbacker.new(name: 'Cally').save(validate: false) }.not_to raise_error
    #end

    #it "raise error on save with validate: true" do
      #expect{ Callbacker.new(name: 'Cally').save(validate: true) }.to raise_error StandardError
    #end
  #end
#end
